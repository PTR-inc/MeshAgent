# Archive dirs under openssl/libstatic/ that no targets.sh target builds and no
# makefile ARCHID links. Kept (not deleted) as historical artifacts; listed here
# so `verify` passes on them while still failing on any NEW orphan.
# One dir per line. Retiring an entry = deleting its line (and the dir).
linux/arm2      # 1.1.1g, 600 objs - predates the current target table
linux/armhf2    # 1.1.1c, 601 objs - superseded by armhf (see targets.sh's armhf note)
linux/poky      # 1.1.1i, 600 objs - Intel Galileo/Yocto 1.6.1, EOL 2016 (targets.sh 'poky' is disabled)
